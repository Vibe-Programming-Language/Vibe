fn fib(n: int) -> int {
    if (n <= 1) { return n; }
    return fib(n - 1) + fib(n - 2);
}

fn main() {
    var n = 10;
    var i = 0;
    while (i <= n) {
        print("fib(" + i + ") = " + fib(i));
        i += 1;
    }
}

